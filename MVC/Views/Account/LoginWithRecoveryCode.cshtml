@model LoginWithRecoveryCodeViewModel
@{
    ViewData["Title"] = "Herstelcode verificatie";
}

<h2>@ViewData["Title"]</h2>
<hr />
<p>
    U hebt gevraagd om in te loggen met een herstelcode. Deze inlog wordt niet onthouden totdat je deze hebt opgegeven
     een authenticator-app-code bij het inloggen of uitschakelen 2FA en opnieuw inloggen.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RecoveryCode"></label>
                <input asp-for="RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Log in</button>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}